#include "main.h"

/**
* div - gets the division of two numbers
* @a: the first number
* @b: the second number
*
* Return: return the division of two numbers
*/
int div(int a, int b)
{
	return (a / b);
}
