#include "main.h"

/**
* mod - get the remainder of a number after division
* @a: the dividend
* @b: the divisor
*
* Return: returns the remainder after division
*/
int mod(int a, int b)
{
	return (a % b);
}
